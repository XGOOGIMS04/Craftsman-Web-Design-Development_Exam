>>>>>> layoutA-3 
- 각각 시멘틱 태그를 사용함
- h1을 준수하기 때문에 
<<<<<
contents는 main 태그를 사용하는데 main 태그는 사이트에서 한번만 사용 가능
contents 안의 내용 같은 경우 독립적이기 때문에 각각 하나의 주제를 의미하는 section 태그를 사용
네비도 nav 태그로 사용
로고는 중요하기 때문에 h1태그 사용
반면 footer의 경우 독립적이지 않기 때문에 div 태그를 사용함
스타일에서 footer는 1과 2가 있고, 2에는 2-1, 2-2가 있음. 2-1과 2-2는 부모가 2이므로 너비가 단독으로 80%가 아닌 부모기준 100%으로 해야함


>>>>>> B 유형
wrap이 100%이기 때문에 wrap에 margin: 0 auto; 필요 없음
헤더와 푸터에는 너비를 100%로 주고 컨테이너를 1200px 로 주고 그 안엔 퍼센트로 자유롭게! + 초반에 네가지로 큰 영역을 잡을땐 너비를 100%로 다해주는게 좋음 
wrap 에는 너비를 100%로 줘야함. 그래야 헤더와 푸터에 남는 영역이 있음.
헤더와 푸터가 아닌 영역에는 margin: 0 auto; 로 가운데 정렬을 해줌
B유형은 헤더와 푸터에 컨데이너 영역이 들어가는거 말곤 A유형과 거의 유사함


>>>>> B-3 
앞의 B1, B2와는 다르게 컨테이너를 넣어주고 이걸 재활용!!!!!!. 그래서 궅이 컨테이너 필요없음
.container {
                width: 1200px;     -> 너비 1200px
                height: inherit;    -> 부모상속받을거
                margin: 0 auto;    -> 가운데 정렬
                background-color: rgba(0, 0, 0, 0.2);  -> 배경색은 rgba로 투명도까지
                display: flex;     -> 자식들 가로정렬
            }
슬라이더는 눈에만 안보이면 되니까 백그라운드만 없애주면 됨
컨텐츠도 배경색 필요 없으니까 백그라운드 없애줌


>>>>> B-4 
B-3 과는 다른점이 스타일에서 헤더 메인 푸터 부분에 컨테이너 없애줌. 어짜피 밑에 컨테이너를 따로 만들어줬기 때문에 적으면 코드가 길어짐. 


>>>>>> C-1 
영역을 크게 왼쪽과 오른쪽으로 2등분 한다. 그래서 왼쪽은 asid, 오른쪽은 main
wrap은 견적서 보고 1000px임
contents 안에는 article로 해도 되고 section으로 해도 됨.
logo 와 nav의 높이는 안정해져있으므로 알아서 (강의에서는 픽셀로 되어있음)
스타일에서 slider, contents, footer는 id 이므로 굳이 #main #slider 로 안적어도 되고 바로 #slider로 적어도 됨


>>>>> C-2
.logo, .nav는 하나밖에 없ㅇ스므로 태그 선택자를 사용해도 됨. -> #aside h1

>>>>>> C-3
.logo, .nav없이  h1, nav 태그만 사용힘. 어짜피 하나니까 클랴스 필요없음

>>>>>> D 유형은 반응형
>>>>>> D-1
우선 크게 aside, main, footer 삼단 구조로 나눔. (푸터에는 LOGO, 글 부분, SNS 세개 일자임)
main은 반응형이니까 화면이 줄어드면 같이 줄어들고 해야함. 전체에서 200px 만큼 빼야하는데 퍼센트랑 픽셀이랑 같이 쓸 수 없으므로 width: calc(100% - 200px); 이렇게 calc()를 사용함
aside, main을 가로정렬했는데 aside와 main 밑에 위치해야히는 footer까지 가로정렬이 됨. 그래서 flex-wrap: wrap;을 사용함. wrap은 부모너비보다 요소들의 총 너비가 크다면 나머지 요소들을 다음줄로 줄바꿈 
링크는 딱히 태그가 없기 때문에 그냥 div를 쓴다
링부분은 애매함. 사용자가 직접 지정해야함. 그러기위해서는 위치를 임의대로 지정할 수 있도록 절대적인 값을 사용함. position: absolute; 를 사용함. 이걸 사용했으면 위치를 지정해줘야하기 때문에 right: 0; top: 0; 을 넣어줌. 그런 후 너비와 높이를 눈대중으로 적어줌 -> absolute를 쓰면 유용하나 너무 자주 쓰면 반응형이 어려워짐
D유형은 여러가지 방법으로 구조를 나눌 수 있음. aside, main 으로 하거나 아니면 footer까지 세개로 하거나. footer에서도 여러개로 나눌 수 있음

>>>>>> D-3
지금은 링크가 오른쪽 위 구석에 위치해있어서 모르겠는데, 만약 배너쪽 오른쪽에 붙으려면 기준점을 줘야함. 안줬을때에는 slider 오른쪽 위로 잡혀서 안만들어짐. 그래서 항상 기준점을 주기 position: relative; 
사실상 D-1도 기준점을 줬어야함. 

>>>>> E-1
위와 아래로 크게 두 구조로 나눈 후 위에는 또 다시 세개의 구조로 나누고 세분화함
main 의 높이도 반응형이여야함. 높이는 %를 인식 못함. 대신 vh를 써줌. 내 화면을 기준으로 해서 처리해줌 -> height: calc(100vh - 120px);  
부모인 main의 높이 값이 정해져있으므로 자식들은 100%로 해도 됨

>>>>> E-2 
main의 높이는 임의 지정이라서 알아서 하는걸로. 강의에선 750px로 함
!!!!1 높이가 임의 지정이므로 꽉 찰 반응형은 아니니까 main의 자식 높이들에게 %로 줄 필요가 없음. 높이는 반응형 아님
> h1은 바로 밑에있는 h1만 선택함

>>>>> F-1 
배너랑 컨텐츠를 합쳐서 contents 로 함. 따로 해도 됨
left: 50%; -> 요소의 왼쪽 끝을 부모의 가로 50% 지점에 둠. 그러면 .link의 부모인 .slider의 중앙에 .link 왼쪽 모서리가 위치하게 됨 
transform: translateX(-50%); -> transform은 자기 자신 기준으로 이동시키는 속성인데, 가로인 x축으로 자기너비의 50%만큼 즉 절반만큼 왼쪽으로 이동함. 


>>>>>. F-2 
contents 부분에 banner, notice, gallery 를 따로 하지 말고 banner 하나랑 div안에 notice, gallery를 넣어줌
div안에 만들어줬으므로 스타일을 표현할때에는 #contents > div .notice  으로 해줘야함. 
위 작업이 어려우면 div에다가 이름을 붙여주면 됨


----------------------------------------------

>>>>> M-1 
서브메뉴는 li 닫기 전에 넣어줘야함. 밖에 넣는것 아님
스타일에선 .nav > ul 을 해줘야 nav바로 아래 ul만 선택됨
 /* nav */
            .nav {}
            .nav > ul {}
            .nav > ul > li {}
            .nav > ul > li > a {}
            .nav > ul > li > ul {}
            .nav > ul > li > ul > li {}
            .nav > ul > li > ul > li > a {}
이렇게 먼저 스타일을 다 적어놓기. 그래야 안헷갈림

1. 먼저 서브메뉴가 있으면 헷갈리기 때문에 .nav > ul > li > ul {display: none;} 을 해준다

2. 메뉴1...을 가로 정렬 하기 위해 각 메뉴 li의 부모인 ul에 .nav > ul {display: flex;} 을 해준다. 그리고    오른쪽으로 정렬을 하기 위해서 justify-content: right; 도 추가해준다. 그리고 밑으로 내려 오기 위해 margin-top을 준다
!!!!이때 justify-content 란? 
-> flex 방향으로 아이템들을 어떻게 정렬할지 정하는 속성. right를 하면 메뉴li들이 오른쪽으로 나란히 선다. 근데 요즘 실무에선 right 보다 flex-end 끝점 오른쪽으로 많이 사용한다

3. .nav > ul > li > a { padding: 10px 50px;} a는 인라인 구조고, padding을 줌으로써 위 양쪽에 패딩값을 준다
!!!!인라인 요소에서 너비, 높이, 줄바꿈은 안됨. 패딩은 가능하고 마진은 좌우만 정상임. 
!!!!메뉴에 패딩을 주는 이유는 a를 클릭했을때 padding이 없으면 딱 붙어있어서 글자만 클릭 가능하고 마우스 올리기도 힘듬. 근데 패딩을 주면 버튼처럼 커져서 클릭 영역이 넓어짐
bgc를 주면 a영역이기 때문에 패딩값이 위아래도 제대로 안먹혀서 이상하게 됨. 이럴때 인라인 구조와 블록 구조를 동시에 가진 인라인 블록을 주면 된다. -> 패딩은 박스 영역이기 때문에 인라인 블록을 줌.

4. 메뉴에 마우스를 올렸을때 배경색을 주기 위해 :hover에 bgc를 넣어준다. 이건 꼭 시험 필수 요소임 .nav > ul > li > a:hover {background-color: #696969;}

5. 이제 서브메뉴를 살린다. 

6. 서브메뉴 a에 패딩값을 주고 꼭 인라인블록을 준다. 너비를 100%로 줬을때 너비에 패딩이 포함되서 오버되는 효과가 있음. 그래서 사이즈를 잴때 패딩값을 포함시키라는 box-sizing: border-box;를 준다.

7.서브메뉴를 가운데로 정렬하기 위해서 ul에 text-align 센터를 준다. 이때 a가 아닌 ul에 주는 이유는 a에 센터를 주면 글자만 가운데로 버튼 위치는 그대로가 됨. ul에다가 주면 서브메뉴 3개 한덩어리를 가운데로 정렬하고 있어서 정렬 기준은 여러개를 감싸는 부모한테 줘야한다.
!!!! a의 상위 박스가 li가 아니라 ul인 이유 : 직접부모는 li가 맞는데 ul에 정렬을 주는 이유는 li는 한줄짜리 껍데기임. li하나는  서브메뉴 하나고 정렬할 대상도 하나가됨. ul은 li를 묶는 박스임. 

8. 서브메뉴의 배경색도 체점 기준이므로 꼭 넣어준다

9. 다 된거같은데 높이 영역을 차지해서 깨짐. 그래서 ul에 position 을 주는데 단점은 영역이 사라짐. 기준점으로 left로 0 을 줌. 그런 후 기준점으로 position relative를 li에게 준다  

10. 100%가 아니기 때문에 ul에 너비를 100%준다

끝~

11. 추가로 서브메뉴 ul에 top을 줘서 조금 띄워준다. 


<!-- 제이쿼리 -->
바디 밑 부분에 제이쿼리 사이트 연결한 후 바로 밑에  <script> $(document).ready(function(){});</script> 이렇게 적어준다. 중괄호 안에 적으면 됨.  스크립트는 동적인 구조를 만들어주기 위해서임

1.  $('.nav > ul > li').mouseover(function(){}) -> 네비에 ul에 li에 마우스를 오버했을때 함수를 작동시켜라. 그 작동은 자식인 서브메뉴를 보여주는 것임. 
$(this).find('.submenu').slideDown() -> 자식을 찾아야하는데 이름이 없으므로 붙여줌.class="submenu. li에 오버했을때 ul class="submenu" 이 부분을 보여줘야함.li에서 .submenu를 찾고 slideDown인 천천히 보여줘라

2. 반대로
$('nav > ul > li').mouseout(function(){
                $(this).find('.submenu').slideUp()
            })
마우스를 아웃했을때 슬라이드업을 해라.

3. 계속 슬라이드 하면 지렁이처럼 계속 왔다갔다 하므로 멈춰라는 .stop을 넣어준다. .stop().slideDown(), .stop().slideUp()


<!--자바스크립트 -->
1.윈도우가 로드됬으면 실행을 시켜라
</script> -->
    <script>
        window.onload = function(){}
    </script>

2. 자바스크립트는 한개가 있을때 네개가 있을때 쓰는방법이 다 다름.
let navList = document.querySelectorAll(".nav > ul > li")
문서에서 .nav > ul > li 를 모두 선택해라. 

3. js에서는 메뉴가 네개가 있고 이를 반복적으로 처리해줘야함. 즉 반복문을 사용해야함 
navList.forEach(function(navItem){})
네개한테 각각 함수를 실행시켜줄껀데 개한테 마우스 오버를 하면 함수를 실행시켜줄꺼임. 코드가 설명하기 복잡해서 적기만 함
navList.forEach(function(navItem){
                navItem.addEventListener("mouseover", function(){
                    this.querySelector(".submenu").style.height = "155px"
                })
            })
155px 준 이유는 js는 애니메이션 기능이 없으므로 우리가 만들어줘야함

4. 위에 이어서 css도 정렬을 해줘야함 


--------------------------------------------------------------------------------------

>>>>>>>M-2
제이쿼리에서 M-1은 this를 사용했다. this 는 .nav > ul > li이므로 지금 마우스가 올라간 li(메뉴 1이면 메뉴1 li),.find('.submenu')는 서브메뉴를 찾아라는 말임. 즉 지금 마우스 올린 li안에서 서브메뉴만 찾아라 이므로 메뉴1에 올리면 메뉴1의 서브메뉴만 내려옴
M-2에서는 '.nav > ul > li > ul' 를 사용함으로써 마우스를 어느 li에 올리든 모든 서브메뉴 ul을 한번에 다운, 떼면 한번에 업

즉, 마우스 오버했을때 하나만 나타나고 싶으면 this, 전체를 다 나타내고 싶다면 .nav > ul > li > ul 사용



--------------------------------------------------------------------------------------

>>>>>>>M-3
전체메뉴의 박스를 만들어줘야하는데 가상 요소를 만들어서 처리해줌. 
#header::after {
                content: '';
                width: 100%;
                height: 0;
                background-color: #808080;
                position: absolute;
                left: 0;
                top: 100px;
            }
#header.on::after {
            height: 155px;
            }
그리고 #header {position:relative} 를 기준점으로 만들어줘야하기때문에 추가로 붙여줌
